configuration:
  mqtt_host:
    name: "MQTT Host"
  mqtt_port:
    name: "MQTT Port"
  mqtt_user:
    name: "MQTT User"
  mqtt_pass:
    name: "MQTT Password"
  use_metric:
    name: "Use Metric system"
  interval:
    name: "Interval"
    description: "Interval in seconds: Seconds between each data retrieval."        